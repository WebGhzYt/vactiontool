<h1><center> Vacation Tool </center></h1>
<h2></h2>
<center>
  <% if user_signed_in? %>
    <% unless current_user.has_access %>
        <div class="jumbotron containerwidth">
          <center>
            <p>You can not visit the Vacation_Tool unless Admin granted your access.</p>
            <p>A notification has been sent to Admin.</p> 
            <p>Please contact Vacation-Tool Admin.</p>
          </center>
        </div>
    <% end %>
  <% else %>
    <p> Sign in with <strong> Your OnGraph id </strong> here </p>
    <%= link_to "Sign in with OnGraph", user_omniauth_authorize_path(:google_oauth2), :class => 'btn btn-lg btn-primary' %> 
  <% end %>
</center>